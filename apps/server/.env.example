# Neon (or any Postgres-compatible) connection string — use the pooled ("-pooler") host.
# If unset, player accounts fall back to an in-memory store (fine for local dev, not for prod).
DATABASE_URL=

# Comma-separated list of origins allowed to call the REST account routes. Leave unset to
# allow all origins (fine for local dev).
ALLOWED_ORIGINS=
